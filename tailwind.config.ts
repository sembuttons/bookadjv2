import type { Config } from "tailwindcss";

/** Optional aliases; most styling uses standard gray/green utilities + hex arbitrary values. */
export default {
  theme: {
    extend: {
      colors: {},
    },
  },
} satisfies Config;
